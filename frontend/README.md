# Centre Formation Oujda — Frontend

Run the frontend dev server:

```bash
cd frontend
npm install
npm run dev
```

The app expects the backend at `http://localhost:4000` by default.

Features:

- product search
- add products to cart
- authenticated checkout
- order confirmation page
- user order history
- admin order management for admin users
